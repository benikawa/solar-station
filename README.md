# solar-station
Remote, solar-powered weather station.

## setup
Run the following commands in the directory of your choice:
```
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```

## run
Run the following command:
```python -m solar-station.main``
